version: 2

models:
  - name: stg_greenery__users
    description: contact informtaion about customers
    columns:
      - name: user_id
        tests:
        - not_null
        - unique
      - name: first_name
        tests:
          - not_null
      - name: last_name
        tests:
          - not_null
      - name: email
        tests:
          - not_null
      - name: phone_number
        tests:
          - not_null
      - name: created_at
      - name: updated_at
      - name: address_id
      #  tests:
      #    - not_null
      #    - unique